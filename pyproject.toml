[project]
name = "distributed-chat-demo"
version = "0.1.0"
description = "Distributed chat demo with multicast discovery, bully election, total ordering, and fault tolerance."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
